# Allow audio HAL to function properly
allow hal_audio_default audio_device:chr_file { read write open ioctl };
allow hal_audio_default audio_prop:property_service set;
allow hal_audio_default hal_audio_default_service:service_manager find;
